name: Axis Engine CI

on:
  push:
    branches: [ main, master, test_cicd ]
  pull_request:
    branches: [ main, master, test_cicd ]

jobs:
  build:
    name: Build (${{ matrix.os }})
    runs-on: ${{ matrix.os }}
    strategy:
      fail-fast: false
      matrix:
        include:
          - os: ubuntu-latest
            preset: linux
            install-cmd: |
              sudo apt-get update
              sudo apt-get install -y ninja-build libglfw3-dev libglm-dev libbullet-dev libfreetype-dev libassimp-dev libenet-dev libavcodec-dev libavformat-dev libswscale-dev libavutil-dev libswresample-dev libopenal-dev
          - os: macos-latest
            preset: macos
            install-cmd: |
              packages=(ninja glfw glm bullet freetype assimp enet ffmpeg)
              missing=()
              for package in "${packages[@]}"; do
                if ! brew list --formula "$package" >/dev/null 2>&1; then
                  missing+=("$package")
                fi
              done
              if [ ${#missing[@]} -gt 0 ]; then
                brew install "${missing[@]}"
              else
                echo "All Homebrew dependencies are already installed."
              fi
          - os: windows-latest
            preset: windows
            install-cmd: "" # Dependencies are pre-committed under lib/ and dlls/

    steps:
      - name: Checkout Code
        uses: actions/checkout@v6
        with:
          lfs: true

      - name: Install System Dependencies
        if: matrix.install-cmd != ''
        run: ${{ matrix.install-cmd }}

      - name: Setup MSVC Developer Command Prompt
        if: matrix.os == 'windows-latest'
        uses: ilammy/msvc-dev-cmd@v1

      - name: Configure CMake
        run: cmake --preset=ci-${{ matrix.preset }}

      - name: Build Engine
        shell: pwsh
        run: |
          $logPath = "build-${{ matrix.preset }}.log"
          cmake --build --preset=ci-${{ matrix.preset }} 2>&1 | Tee-Object -FilePath $logPath
          $exitCode = $LASTEXITCODE
          if ($exitCode -ne 0) {
            $tailLines = Get-Content $logPath -Tail 160 |
              Where-Object { $_ -notmatch '^\s*/usr/bin/(c\+\+|cc|clang\+\+|clang) ' -and $_ -notmatch '^\s*".*\\(cl|link)\.exe" ' }
            $tail = $tailLines -join "`n"
            $message = $tail.Replace('%', '%25').Replace("`r", '%0D').Replace("`n", '%0A')
            Write-Host "::error title=Build failed (${{ matrix.os }})::$message"
            exit $exitCode
          }

      - name: Run Tests
        run: ctest --preset=ci-tests-${{ matrix.preset }} -C Release
