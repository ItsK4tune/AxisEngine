#pragma once

// Stable application-facing surface. Prefer this header for games and tools;
// backend implementation headers under engine/*/strategy are intentionally
// not installed as part of the SDK.
#include <axis_app.h>
#include <axis_build.h>
#include <axis_component.h>
#include <axis_types.h>
